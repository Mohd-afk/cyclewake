/**
 * STUB: Bottom navigation bar for future multi-page app.
 *
 * EXTENSION: When dashboard, history, and health pages are built,
 * this component will render navigation icons at the bottom of
 * the screen (mobile-first tab bar pattern).
 *
 * Not rendered in MVP — exists as an architecture placeholder.
 */

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const NAV_ITEMS = [
  { href: '/', label: 'Calculator', icon: '⏰' },
  { href: '/dashboard', label: 'Dashboard', icon: '📊' },
  { href: '/history', label: 'History', icon: '📅' },
  { href: '/health', label: 'Health', icon: '💚' },
];

export default function BottomNav() {
  // EXTENSION: Uncomment and implement when multi-page nav is needed
  return null;
}
